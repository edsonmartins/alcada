package app.alcada.captura.internal;

/** Tipos de job do scheduler usados pela captura. */
final class TiposJob {
    static final String PROCESSAR_CAPTURA = "PROCESSAR_CAPTURA";

    private TiposJob() {
    }
}
